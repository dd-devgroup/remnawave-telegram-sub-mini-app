import { Badge } from '@mantine/core'

export default {
    Badge: Badge.extend({
        defaultProps: {
            radius: 'xl',
            variant: 'light',
            color: 'sunset',
            fw: 600
        }
    })
}
